{
  "name": "crashreport-server",
  "version": "1.0.0",
  "description": "Server to receive crash reports from Electron",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.2",
    "express": "^4.16.1",
    "http": "0.0.0",
    "minidump": "^0.11.0",
    "multer": "^1.3.0",
    "raven": "^2.2.1"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  },
  "nodemonConfig": {
    "ignore": [
      "app-crashes/*"
    ]
  }
}
